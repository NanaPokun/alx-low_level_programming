#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Description: The function would print the exact text provided
 */

int main(void)
{
	/**
	 * The main printf function starts here
	 * return is always 0
	 */
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
